name: flutter_project_pfa
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🌐 HTTP API (FastAPI predict)
  http: ^1.2.1

  # 📷 CAMERA + IMAGE
  camera: ^0.12.0+1
  image_picker: ^1.1.2

  # 🔐 PERMISSIONS (camera, storage)
  permission_handler: ^12.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true